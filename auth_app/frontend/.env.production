# ── Production Environment ──
# All routes (/login, /dashboard, /admin, /admin/login) live on the same origin.
# The /api/* calls are proxy-rewritten to the backend by Vercel (see vercel.json).
VITE_API_BASE_URL=/api
