# Runtime requirements for NyayaDepaaAI AI Chatbot service (serving only)
# Excludes training/fine-tuning deps (torch, transformers, etc.)

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# LLM Providers
groq>=0.9.0
google-generativeai>=0.5.0
requests>=2.31.0

# Retrieval - Pinecone (cloud vector DB)
pinecone-client>=3.2.0

# Case analysis - ChromaDB (local vector DB)
chromadb>=0.4.0

# Embeddings via API (no local models)
numpy>=1.26.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1
python-multipart>=0.0.6
